import time





def create_training_files(files, file_handler, bandpass_f, channels = None, order = 4, filename = None):
    
    if filename is None:
        
    
    

    
class BasicFileHandler:
    
    def __init__(self, fs, order = 'row', dtype = np.int16):
        self.fs = fs
        self.order = order
        self.dtype = dtype
        
    def __call__(self, file, 


def circular_loss(y_true, y_pred):
    y_true = tf.cast(tf.reshape(y_true, [-1]), tf.float32)
    return 1 - tf.cos(angle - y_true)
  
## training_dataset function

### data generators


